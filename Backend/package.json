{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend for fullstack application",
  "main": "index.js",
  "scripts": {
    "build": "npm run build-frontend && rm -Rf dist && mv ../frontend/dist . && npm run build-backend",
    "build-frontend": "npm run --prefix ../frontend build",
    "build-backend": "tsc",
    "start": "node dist/index.js",
    "dev": "nodemon --exec 'ts-node' index.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "pg": "^8.7.1"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.2",
    "@types/node": "^22.5.0",
    "nodemon": "^2.0.20",
    "ts-node": "^10.9.1",
    "typescript": "^5.5.4"
  }
}
