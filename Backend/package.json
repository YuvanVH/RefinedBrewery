{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend for fullstack application",
  "main": "index.js",
  "scripts": {
    "start": "node dist/index.js",
    "dev": "nodemon --exec 'ts-node' index.ts",
    "build-frontend": "ls -l ../ && ls -l ../frontend && cd ../frontend && npm install && npm run build && cd ../backend && rm -Rf dist && cp -R ../frontend/dist dist",
    "build": "tsc"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "pg": "^8.7.1"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.2",
    "nodemon": "^2.0.20",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
